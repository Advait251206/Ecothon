{
  "name": "ecothon-hyacinthwatch",
  "version": "1.0.0",
  "description": "HyacinthWatch Monorepo",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend_user\" \"npm run dev --prefix frontend_admin\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend_user\" \"npm start --prefix frontend_admin\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend_user && npm install --prefix frontend_admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
